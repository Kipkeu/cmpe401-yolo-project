# CMPE 401 - YOLO11 Object Detection on VisDrone
ECHO is on.
This repository contains baseline training, experiments, and analysis for the VisDrone dataset using Ultralytics YOLO11.
ECHO is on.
## Structure
- `configs/` YAML configs for runs
- `data/visdrone.yaml` Dataset config (paths only; dataset not included
